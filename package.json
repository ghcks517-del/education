{
  "name": "safe-ed-guide-pwa",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R index.html app.js styles.css manifest.webmanifest service-worker.js public icons dist/"
  },
  "devDependencies": {}
}
